import 'package:flutter/material.dart';

class FirstPart extends StatelessWidget {
  const FirstPart({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Login or register to book your appointments',style: TextStyle(fontSize: 24,fontWeight: FontWeight.bold),);
  }
}

class TextfieldPart extends StatelessWidget {
  const TextfieldPart({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Login or register to book your appointments',style: TextStyle(fontSize: 24,fontWeight: FontWeight.bold),);
  }
}
